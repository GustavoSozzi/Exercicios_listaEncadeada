

/*Lista *busca(celula *head, int valor)
{
    if(head->valor == valor)
    {
        return NULL;
    }
    else if(head->prox == NULL) //se a proxima posiçao é vazia
    {
        return NULL;
    }
    else
    {
        return busca(head->prox, valor);
    }
}*/


//Busca em uma lista encadeada com fila de prioridade;

//FILAS CIRCULARES
//FILA COM PRIORIDADE
        /*HEAP*/
        /*PRIORIDADE ORDENADA*/
        /*PRIORIDADE COM ORDENAÇÃO*/


